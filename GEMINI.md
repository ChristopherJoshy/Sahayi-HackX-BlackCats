# SAHAYI Project Rules

## Backend Edit Rule
If you want to edit the backend, create a backend edit request called `request{id}.md`. For the `id`, increment it each time. Create a folder called `requests` and put the file inside it. Always push these request files to git.
